# El pipeline la lee con python-dotenv / os.environ -- nunca esta escrita en el codigo.
# Se usa solo en qa_llm_generate.py, para la parte del Q&A generada por LLM (no en build-corpus).
# Copiar este archivo a .env y completar con una clave real:
#   copy .env.example .env          (Windows)
#   cp .env.example .env            (macOS/Linux)
ANTHROPIC_API_KEY=
